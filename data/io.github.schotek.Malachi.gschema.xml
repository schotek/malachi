<?xml version="1.0" encoding="UTF-8"?>
<schemalist gettext-domain="malachi">
  <!--
    UI-only preferences, read by ui/internal/settings. Anything that affects
    mail handling (accounts, sync intervals, remote-content policy) belongs to
    the backend's config.toml and is set through the RPC API, not through
    GSettings.

    Development builds compile this schema into build/glib-2.0/schemas and
    export GSETTINGS_SCHEMA_DIR (see Makefile and scripts/dev-run.sh). When
    the schema cannot be found at runtime the UI falls back to an in-memory
    store and logs a warning; it never aborts.
  -->
  <enum id="io.github.schotek.Malachi.ColorScheme">
    <value nick="system" value="0"/>
    <value nick="light" value="1"/>
    <value nick="dark" value="2"/>
  </enum>
  <enum id="io.github.schotek.Malachi.Density">
    <value nick="comfortable" value="0"/>
    <value nick="compact" value="1"/>
  </enum>

  <schema id="io.github.schotek.Malachi" path="/io/github/schotek/Malachi/">
    <!-- Window geometry -->
    <key name="window-width" type="i">
      <default>1200</default>
      <summary>Window width</summary>
    </key>
    <key name="window-height" type="i">
      <default>760</default>
      <summary>Window height</summary>
    </key>
    <key name="window-maximized" type="b">
      <default>false</default>
      <summary>Window maximized state</summary>
    </key>
    <key name="folder-pane-width" type="i">
      <default>240</default>
      <summary>Width of the folder sidebar</summary>
    </key>
    <key name="message-list-width" type="i">
      <default>380</default>
      <summary>Width of the message list pane</summary>
    </key>

    <!-- Sidebar state. Which parts of the folder tree the user folded away
         and which folders are pinned to the Favourites section; one entry
         per node, encoded by ui/internal/window. Purely presentational,
         hence GSettings and not the daemon. -->
    <key name="collapsed-folders" type="as">
      <default>[]</default>
      <summary>Folders whose subfolders are hidden in the sidebar</summary>
    </key>
    <key name="collapsed-accounts" type="as">
      <default>[]</default>
      <summary>Accounts whose folders are hidden in the sidebar</summary>
    </key>
    <key name="favourite-folders" type="as">
      <default>[]</default>
      <summary>Folders pinned to the Favourites section of the sidebar</summary>
    </key>

    <!-- General (UI behaviour). Mail options such as the sync interval and
         the remote-content policy are daemon-owned: config.get/config.set. -->
    <key name="launch-at-login" type="b">
      <default>false</default>
      <summary>Start hidden in the background at login (mirrors the Background portal's autostart decision)</summary>
    </key>
    <key name="run-in-background" type="b">
      <default>false</default>
      <summary>Closing the main window keeps the application running</summary>
    </key>
    <key name="mark-read-delay" type="i">
      <default>2</default>
      <range min="0" max="60"/>
      <summary>Seconds a message must be shown before it is marked as read; 0 = immediately</summary>
    </key>
    <key name="confirm-delete" type="b">
      <default>true</default>
      <summary>Ask before moving a message to Trash</summary>
    </key>
    <key name="desktop-notifications" type="b">
      <default>true</default>
      <summary>Show a desktop notification for new mail</summary>
    </key>
    <key name="notification-sound" type="b">
      <default>false</default>
      <summary>Play the system new-mail sound</summary>
    </key>

    <!-- Appearance -->
    <key name="color-scheme" enum="io.github.schotek.Malachi.ColorScheme">
      <default>'system'</default>
      <summary>Color scheme</summary>
      <description>Follow the system preference, or force a light or dark appearance.</description>
    </key>
    <key name="message-list-density" enum="io.github.schotek.Malachi.Density">
      <default>'comfortable'</default>
      <summary>Message list density</summary>
    </key>
    <key name="show-preview-line" type="b">
      <default>true</default>
      <summary>Show a one-line preview under each message in the list</summary>
    </key>
    <key name="group-by-conversation" type="b">
      <default>false</default>
      <summary>Group the message list by conversation</summary>
      <description>Show one row per conversation in the selected folder; expanding it lists the messages the folder holds.</description>
    </key>
    <key name="show-avatars" type="b">
      <default>true</default>
      <summary>Show sender avatars in the message list</summary>
    </key>
    <key name="monochrome-avatars" type="b">
      <default>false</default>
      <summary>Draw avatars in neutral grey instead of per-sender colours</summary>
    </key>
    <key name="monospace-plain-text" type="b">
      <default>false</default>
      <summary>Use a monospace font for plain-text message bodies</summary>
    </key>
    <key name="text-zoom" type="i">
      <default>100</default>
      <range min="50" max="200"/>
      <summary>Message body text zoom in percent</summary>
    </key>
  </schema>
</schemalist>
